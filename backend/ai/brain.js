export async function brainResponse(message) {
  if (!message) {
    return "तुम्ही काहीच विचारलं नाही. कृपया सांगा 🙂";
  }

  const lower = message.toLowerCase();

  if (lower.includes("काय मदत")) {
    return "मी माहिती, प्लॅन, शॉपिंग, प्रॉपर्टी, मार्केटिंग आणि पुढे रियल कामे करू शकतो.";
  }

  if (lower.includes("screen") || lower.includes("live")) {
    return "ठीक आहे. LIVE मोड पुढच्या स्टेपमध्ये सुरू करू.";
  }

  return `मी समजलो: "${message}". पुढे काय करायचं आहे ते सांगा.`;
}
